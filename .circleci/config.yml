version: 2

jobs:

  test:
    docker:
      - image: circleci/golang:1.12

    working_directory: /go/src/cinema-admin
    steps:
      - checkout

      - run: make setup-package
      - run: make test

  build:
    working_directory: /go/src/cinema-admin
    docker:
      - image: circleci/golang:1.12
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run: make setup-package
      - run: docker build -t $IMAGE_NAME .
      - run:
          name: Publish Docker Image to Docker Hub
          command: |
            echo "$DOCKERHUB_PASS" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            docker push $IMAGE_NAME:latest
  
  deploy: 
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "f5:0c:d0:f7:97:32:ff:2c:56:60:0d:ff:ee:f4:b3:23"
      - run: ssh root@$VM_IP  
      # - run: cd cinema-admin
      # - run: docker-compose -f app.yml down
      # - run: docker-compose -f app.yml up -d

    #   - run: apk --update add openssh-client
    #   - run: eval $(ssh-agent -s)
    #   - run: cat deploy/deploy_key_prod | tr -d '\r' | ssh-add - > /dev/null
    # - ssh-add -l
    # - mkdir -p ~/.ssh
    # - echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config
    # - apk add socat

workflows:
  version: 2
  build-test-and-deploy:
    jobs:
      - test
      - build:
          requires:
            - test
      - deploy:
          requires:
            - build